features
    "spin"
        optional
            "lock_api_crate"
                optional
                    "lock_api"
                        optional
                            "default"
            "use_ticket_mutex"
            "ticket_mutex"
            "std"
            "spin_mutex"
            "rwlock"
            "portable_atomic"
            "portable-atomic"
            "once"
                optional
                    "lazy"
            "mutex"
                optional
                    "fair_mutex"
                    "barrier"
constraints
    "lazy" => "spin"
    "default" => "spin_mutex" & "rwlock" & "lazy" & "once" & "mutex" & "barrier" & "spin"
    "spin_mutex" => "mutex"
    "barrier" => "spin"
    "ticket_mutex" => "mutex"
    "use_ticket_mutex" => "ticket_mutex" & "mutex"
    "portable_atomic" => "portable-atomic"
    "fair_mutex" => "spin"
    "lock_api" => "spin"
